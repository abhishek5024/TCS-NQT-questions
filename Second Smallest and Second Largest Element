#include <iostream>
#include<bits/stdc++.h>
using namespace std;
void smallar(int arr[], int n)
{
  sort(arr, arr+n);
  int largest = arr[1];
  int smallest = arr[n-2];
  cout<<largest<<" "<<smallest;
}
int main() {
 int arr[100]={11,0,3,4,1,34};
 int n=6;
 smallar(arr,n);
  return 0;
}
